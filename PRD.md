# PRD — 产品需求

> 状态：MVP 设计稿（2026-08-02）
> 配套文档：[ARC.md](./ARC.md)（技术架构）· [MRD.md](./MRD.md)（市场与商业）· [AGENT.md](./AGENT.md)（项目总览）

## 1. 产品概述

**dailog——一档"基于 AI 对话剪辑"的访谈式播客**（定位与商业逻辑见 MRD §1/§5）。

- **核心差异**：基于 AI 对话剪辑——原料是**真实发生的人机对话**（不是剧本、不是 AI 生成的对谈），且这份原料人人都已拥有（对话历史）——投稿零门槛的根源
- **形态**：访谈式，每期 5–10 分钟，呈现人与 AI 对话中的精华——按**内容价值四维**（交锋/新知/情感/经验，定义见 MRD §1「内容承诺」）
- **来源**：投稿人分享的**真实 AI 对话**（分享链接 + 声音采样，平台清单见 §4.3）——润色管线把原始对话加工成访谈结构（开场白、串联词、去芜存菁）
- **制作**：投稿人 = 采访者（自己的克隆音色），AI = 受访嘉宾（dailog 固定品牌声线）→ 发布到 dailog 单 feed，全平台同步更新（v1 RSS 系：Apple/Spotify/小宇宙）

## 2. 目标用户与场景

- AI 重度用户：与 AI 的深度对话值得被听见、被收录（投稿人）
- 想"被听见"的人：零门槛拥有"上过 dailog"的公开作品（投稿人）
- 听众：对真实 AI 对话好奇、想听"人和 AI 聊天精华"的人
- 内容创作者：把 AI 对话素材变成被品牌背书的公开作品（投稿人）

（投稿人画像详见 MRD §2.1）

## 3. 核心流程

```
【投稿人】（dailog.fm）注册（开放，邮箱验证即获投稿资格）→ 投稿：粘贴分享链接 → 采集预览 + 配置人设（主持人信息 + 声音采样，已有自动填充）
→ 提交投稿 → /me/submits 查看状态（审核中）
【编辑】（admin.dailog.fm）查看投稿队列 → 触发大模型审核 + 润色 → ✗ 不通过：标记投稿失败（附原因）
→ ✓ 通过：选择脚本（可手工修改）→ 生成语音 → 自动发布（播放页 + 单 feed RSS + 投稿人作品集）
【听众】订阅 dailog 单 feed → 收听 / 分享
```

## 4. MVP 功能清单

### 4.1 账户与角色

- better-auth（自托管）：邮箱 + 密码 + 会话；**注册开放 + 邮箱验证**，验证通过即获得投稿资格（**无邀请码**）
- **角色**：`profiles.role` = user（投稿人）/ editor（编辑）/ admin（管理员）；**投稿人端在 dailog.fm（site）**，注册即可投稿；**编辑/管理员登录 admin.dailog.fm（管理员工作台）**，驱动审核/润色/生成/发布
- 边界：垃圾投稿的兜底 = 邮箱验证 + 人工审核队列（§4.5）；v1 不做投稿频率限制，观察队列垃圾率后再加（配置化）

### 4.2 人设（主持人信息 + 声音采样）

- **人设 = 投稿人的主持人配置**：显示名 + 简介（可选）+ 声音采样（30–60 秒，波形可视化、试听、重录、质量校验：时长/响度/语音检测）
- **配置时机：投稿确认环节**（采集预览后）；已配置过则自动填充、可修改
- 声音 = 投稿人克隆音色（上传录音样本后即时可用：优先创建 Fish fast 音色模型（训练 5–8 秒、免费，模型 id 存 `voice_samples.reference_id`）；创建失败降级 **Fish Audio 零样本按需克隆**——录音样本随每次 TTS 请求携带（msgpack 内联），**重录即时生效**）；嘉宾 = dailog 固定品牌音色（音色库模型 id）
- 人设可随时在设置页重录/修改

### 4.3 导入（分享链接采集服务）

**概念模型**：
- **快照（snapshot）**：分享 URL 的内容提取——全局资源（与用户无关，URL 唯一；内容固定永久有效）
- **创作容器（polish）**：用户 × 快照 的工作区（唯一约束——重复粘贴跳转编辑页，语义=继续创作）
- **润色脚本（transcript）**：polish 可包含多个（每次润色一条，无版本概念）
- **节目（episode）**：由一条润色脚本生成
- **音轨（track）**：节目音频——单语言（跟随对话语言）；**不做单节目多语言音轨**：对话翻译后的版本本质是"基于同一快照的新节目"（新脚本新音轨），不是同一期的 tracks

- **唯一导入通道：分享链接**——用户粘贴 AI 平台对话的**分享链接**（无需登录态、无需安装任何东西），服务端采集后在工作台预览确认
- **快照写入分层**：采集触达失败（网络/CF）写快照并 10 分钟可重试；**解码失败（内容为空）不写库**——通常是平台结构变化导致解析器失效，修复后自动重试恢复，避免坏缓存永久污染
- **服务端采集服务（`importer.dailog.fm`）**：按平台解析分享页数据（公开接口 / SSR 内嵌数据 / RSC payload），无需用户登录、无需浏览器；平台规则变化只更新采集服务，不影响主站
- **平台（按采集通道）**：Claude（chat_snapshots 公开接口，CF 拦截时走 ScraperAPI）、DeepSeek（share/content API）、ChatGPT（RSC payload 解码）、Gemini（batchexecute RPC）、Kimi（SSR HYDRATION_INIT_STATE）、豆包（SSR 快照，海外出口用香港代理）
- **体验**：粘贴分享链接 → 采集预览（标题/条数/全文）→ 确认入库 → 工作台编辑发布
- **元数据**：分享页标题（预填节目标题）、对话 ID + 原始链接（来源展示、重复导入识别）、平台标识
- 解析预览：确认对话结构、修正说话人映射（AI 说话人识别不准确时可手动改）
- ~~浏览器扩展采集（登录态 DOM 采集）~~：**已停用**——源码保留在 `apps/extension`，不再作为导入通道

### 4.4 审核与润色（编辑端）

- **编辑触发**：编辑对投稿执行"大模型审核 + 润色"——审核按收录标准执行（内容价值四维**任一成立即可通过**：① 交锋（观点/立场碰撞与反转，含 AI 出人意料回应）② 新知（知识/信息差/前沿认知）③ 情感（共鸣/情绪故事/真实情感流动）④ 经验（方法与实操含 AI 使用技巧/避坑/决策推演）；拒绝条件：对话过短（<3 轮）、纯寒暄无主题、既无认知价值也无情感价值、涉及违规内容）——**不通过 → 标记投稿失败（附原因，通知投稿人）**
- **润色**：LLM 把原始对话剪辑加工成**访谈式结构**（开场白、串联词、压缩到 5–10 分钟、收尾），SSE 流式返回脚本；**编辑可多次触发润色（带方向指示，如"更简短""换个开场"），生成多版候选**
- **脚本编辑**：编辑可手工修改脚本（改文本、增删段落、改说话人、折叠对照原始对话）；重新润色覆盖前先确认（防误覆盖）
- **字数上限**：脚本硬上限 5000 字（润色输出超限自动截断兜底）——**用户侧无任何润色上限**（润色仅编辑触发）
- 润色语言跟随对话内容自动检测，与界面语言无关；界面中英双语可切换
- 片头片尾：固定预制音频，按对话语言自动选择（中文/英文），无需配置

### 4.5 生成与发布（编辑端）

- **生成**：编辑选择脚本后触发——先过**内容安全审核**（最终脚本，色情/违法/仇恨/诈骗等，拒绝则 422 + 原因、不生成——品牌命门）→ 进度条展示 TTS → 合并 → 上传
- **发布**：生成完成后**自动发布**到 dailog 单 feed（标题/简介/封面由编辑确认或自动生成默认图）→ 播放页 + RSS + 投稿人作品集即时生效
- **封面（图库匹配）**：润色时 LLM 输出封面关键词（~0 成本）→ 图库 API（Pexels，免费额度）搜索候选 → **编辑工作台展示 4 张候选供选择** → 下载存 R2 → `cover_url`；跳过/失败则用渐变模板（零成本兜底）；`PEXELS_API_KEY` 未配置时直接走模板
- **精选标记**：编辑可标记精选节目（`episodes.is_picked`）——首页首屏播放器与 `/discover/picked` 共用该池（首页取最新精选）
- **拒绝**：编辑可随时手工拒绝投稿（标记原因，通知投稿人）
- **投稿状态机（承载于 polishes.status）**：`submitted`（审核中）→ `rejected`（投稿失败）/ `accepted`（已收录）→ 制作发布（episodes：`generating` → `published`）。**episodes 仅在生成后才创建**（transcriptId NOT NULL——投稿阶段尚无脚本）

### 4.6 频道与内容站

- **dailog 单一品牌频道**（首页 = 频道本体）：最新节目流 + 热门 + 搜索
- 节目页：播放器、简介、润色后脚本全文、下载 MP3、分享、**查看原文**——对话全文（数据源 `imports.parsed_dialogue`，导入即全量保存），仅供已收录节目公开访问（`getPublishedDialogue` 预留，草稿不可见）
- **投稿人主页 `dailog.fm/@username`**：投稿人资料 + 被收录作品集（"上过 dailog"）
- **单 feed RSS**（`dailog.fm/feed.xml`）：进 Apple/Spotify/小宇宙；分语言 feed（zh/en）为 v2
- 未收录 = 草稿（仅投稿人自己可见），无私密节目概念

### 4.7 成本预算（v1 无创作者收费，编辑驱动）

**v1 无任何收费点**——投稿免费、收录免费；成本控制 = **编辑决策（用稿率）** + 硬上限（商业逻辑见 MRD §5）。

- **编辑决策前置**：大模型审核 + 润色由编辑对具体投稿触发（§4.4）——被拒投稿只产生审核成本（~¥0.1），只有通过者产生制作成本（~¥1/期）——**编辑的用稿率 = 成本总开关**（例：100 投稿/天 × ¥0.1 审核 + 20 通过 × ¥1 制作 ≈ ¥30/天 ≈ ¥900/月）
- **成本上限（配置化）**：全站脚本硬上限 5000 字（润色输出截断兜底，生成前校验超限提示精简）；单次输入对话量上限（单请求成本封顶）；免费层 TTS 用 `s2.1-pro-free`（$0）——**无润色次数限制**（润色仅编辑触发，成本由编辑决策兜底）
- **生成前内容安全审核（§4.5）**：不通过不进入制作（品牌频道命门——一期事故 = 品牌受损，见 MRD §5）
- **v2 听众侧变现**（频道资产验证后，MRD §5）：Apple/小宇宙会员 + 打赏与入选投稿人分成——不在 MVP 范围

## 5. 页面清单

### 管理员工作台（admin.dailog.fm，仅 admin/editor 登录）

| 页面 | 内容 |
|---|---|
| `/login` | 编辑/管理员登录（普通用户无权限） |
| `/queue` | 投稿队列（待审核/已收录/已发布） |
| `/reviews/:id` | 审核详情：触发审核 + 润色 → 选脚本（可手工编辑）→ 生成 → 发布 / 拒绝 |
| `/settings` | 嘉宾音色管理、账号 |

### 内容站 SSR + 投稿人端（dailog.fm，无编辑路由）

| 路由 | 内容 |
|---|---|
| `/` | 首页（landing）：首屏左列 = tagline + 一句话 what it is + 「立即投稿」CTA；右列 = 精选节目播放器 |
| `/discover` · `/discover/<new\|hot\|picked\|top>` | 探索页：新 / 热 / 精 / 荐 |
| `/tags` | 标签聚合（分组展示；标签来自润色元数据 tags） |
| `/episodes?params=…` | 搜索结果（关键词/标签/语言等查询参数） |
| `/submit` | **投稿流程**：①导入（分享链接 + 采集预览）→ ②配置人设（主持人信息 + 声音采样，已有自动填充）→ ③提交投稿 |
| `/me` | 个人中心入口 |
| `/me/submits` | 我的投稿及状态（审核中/已收录/投稿失败） |
| `/me/episodes` | 我的作品集（已收录节目） |
| `/me/favorites` · `/me/likes` | 我的收藏 / 我喜欢 |
| `/account` · `/account/settings` | 账户与设置（邮箱/密码/通知） |
| `/account/reset-password` | 重置密码 |
| `/login` | 登录/注册（邮箱验证） |
| `/@<username>` | 投稿人主页（默认邮箱前缀为用户名，冲突加后缀） |
| `/episode/<id>` | 播放页（单集） |
| `/hosts` · `/guests` | 主持人（投稿人）聚合 / dailog AI 嘉宾展示 |
| `/feed.xml` | 播客 RSS（单 feed，进 Apple/Spotify/小宇宙） |

## 6. 内容与语言规则

- 对话语言检测：LLM 润色前自动检测（zh/en 为主），脚本语言 = 对话语言
- 界面语言：中英切换，与内容无关
- 片头片尾：按对话语言选 zh/en 固定资产
- **v1 单 feed 混排多语言**（语言跟随对话内容）；分语言 feed（zh/en）为 v2
- **单期时长：5–10 分钟**。润色以该时长为目标压缩（对话过长时自动选取最有价值的内容）；仍超上限提示用户拆分或截取；脚本 ≤ 80 段

## 7. 非目标（v2+）

- 文本粘贴 / 导出文件 / ~~分享链接导入~~（分享链接现为唯一导入通道，见 §4.3）
- 背景音乐 / 音效 / 片头片尾自定义生成
- 点赞、评论、关注等社交互动
- 自动内容审核（v1 仅"举报 + 下架"接口）
- 投稿人子频道 / 多语言分 feed（v2，矩阵式扩张）
- 播客统计（收听量、订阅数）
- 移动端原生 App

## 8. MVP 成功指标（建议，投稿制）

- 投稿漏斗：注册 → 邮箱验证 → 提交投稿 各步转化率
- 投稿-收录漏斗：投稿量 → 编辑审核处理量 → 收录量（编辑用稿率，验证收录标准松紧与成本预算）
- 编辑吞吐：投稿到审核的平均时效（人工驱动的瓶颈指标）
- 入选者行为：入选后自发分享率（荣誉驱动假设，MRD §9-3）
- 听众指标：频道订阅数、单集收听/完播、回访（频道资产验证，MRD §9-4）
- 供给可持续：每周投稿量 vs 每周收录期数（维持 1–2 期/周，MRD §9-5）

## 9. 已知边界与限制

- 对话过长需拆分或截取（单期目标 5–10 分钟，约 1200–3000 字）
- 导入依赖各平台分享页公开数据通道（平台接口/页面结构变化时更新采集服务，属持续维护项）
- 克隆音色质量依赖用户录音环境（投稿确认环节校验）
- 成本失控风险由编辑决策 + 硬上限控制（MRD §5）；多账号刷投稿由邮箱验证 + 人工审核兜底（垃圾只占队列，不产生制作成本）
- 收录内容为用户生成内容（UGC），需举报下架机制；**品牌频道 = 品牌风险面，内容安全审核为命门**（MRD §5）；编辑人工策展是 v1 的收录质量兜底
