# PRD — 产品需求

> 状态：MVP 设计稿（2026-08-02）
> 配套文档：[ARC.md](./ARC.md)（技术架构）· [MRD.md](./MRD.md)（市场与商业）· [AGENT.md](./AGENT.md)（项目总览）

## 1. 产品概述

**把用户与 AI 的对话发布为二人对谈播客**：用户是主持人（用自己的克隆音色），AI 是嘉宾（固定音色）。

- 输入：一段用户与 AI 的对话（分享链接采集，平台清单见 §4.3）
- 加工：LLM 润色成播客脚本（开场白、串联词、理顺口语、压缩长段），用户可编辑
- 输出：一档托管播客（播放页 + RSS 订阅 + MP3 下载）
- 每个用户 = 一个播客频道：`dailog.fm/@username`

## 2. 目标用户与场景

- AI 重度用户：与 AI 的深度对话值得保存、分享、重听
- 内容创作者：把 AI 对话素材变成可发布的音频内容
- 想"被听见"的人：零门槛拥有自己的播客频道（不需录音棚、不需剪辑）

## 3. 核心流程

```
注册（开放）→ 开通频道（邀请码）→ 录声音样本 → 导入对话 → 润色脚本（可编辑）
→ 生成（TTS+拼接）→ 试听 → 发布（标题/简介/封面）→ 播放页 + RSS 分发 → 社区站可被发现
```

## 4. MVP 功能清单

### 4.1 账户与频道开通（邀请码）

- better-auth（自托管）：邮箱 + 密码 + 会话；**注册完全开放**（人人可注册）
- **频道开通**：生成/发布需先用邀请码开通频道（未开通时接口 403 channel_not_active，SPA 引导开通页）
- 邀请码来源：
  - **管理员**：CLI 生成（可设过期时间）
  - **用户奖励**：已发布期数 > 3 起，每发布一期节目 +1 邀请码，在设置页可查看/分享
- 边界：已发放邀请码不回收（取消发布不收回）；**前 3 期不补发**；一个码只能用一次

### 4.2 录音与声音

- 注册引导页录音 30–60 秒（波形可视化、试听、重录）
- 质量校验：时长、响度、语音检测（过差提示重录）
- 主持人声音 = 克隆音色（上传录音样本后即时可用：优先创建 Fish fast 音色模型（训练 5–8 秒、免费，模型 id 存 `voice_samples.reference_id`）；创建失败降级 **Fish Audio 零样本按需克隆**——录音样本随每次 TTS 请求携带（msgpack 内联），**重录即时生效**）；嘉宾 = 平台固定音色（音色库模型 id）
- 可随时重录覆盖

### 4.3 导入（分享链接采集服务）

**概念模型**：
- **快照（snapshot）**：分享 URL 的内容提取——全局资源（与用户无关，URL 唯一；内容固定永久有效）
- **创作容器（polish）**：用户 × 快照 的工作区（唯一约束——重复粘贴跳转编辑页，语义=继续创作）
- **润色脚本（transcript）**：polish 可包含多个（每次润色一条，无版本概念）
- **节目（episode）**：由一条润色脚本生成
- **音轨（track）**：节目的多语言音频（预留）

- **唯一导入通道：分享链接**——用户粘贴 AI 平台对话的**分享链接**（无需登录态、无需安装任何东西），服务端采集后在工作台预览确认
- **快照写入分层**：采集触达失败（网络/CF）写快照并 10 分钟可重试；**解码失败（内容为空）不写库**——通常是平台结构变化导致解析器失效，修复后自动重试恢复，避免坏缓存永久污染
- **服务端采集服务（`importer.dailog.fm`）**：按平台解析分享页数据（公开接口 / SSR 内嵌数据 / RSC payload），无需用户登录、无需浏览器；平台规则变化只更新采集服务，不影响主站
- **平台（按采集通道）**：Claude（chat_snapshots 公开接口，CF 拦截时走 ScraperAPI）、DeepSeek（share/content API）、ChatGPT（RSC payload 解码）、Gemini（batchexecute RPC）、Kimi（SSR HYDRATION_INIT_STATE）、豆包（SSR 快照，海外出口用香港代理）
- **体验**：粘贴分享链接 → 采集预览（标题/条数/全文）→ 确认入库 → 工作台编辑发布
- **元数据**：分享页标题（预填节目标题）、对话 ID + 原始链接（来源展示、重复导入识别）、平台标识
- 解析预览：确认对话结构、修正说话人映射（AI 说话人识别不准确时可手动改）
- ~~浏览器扩展采集（登录态 DOM 采集）~~：**已停用**——源码保留在 `apps/extension`，不再作为导入通道

### 4.4 润色编辑器

- **质量审核前置**：打磨前先按审核标准自动预检（轻量 LLM），以下情况直接拒绝并返回原因：对话过短（<3 轮）、纯寒暄无主题、信息量不足、涉及违规内容——不进入打磨与生成（省成本、控质量）
- **生成前内容安全审核**：用户手动编辑脚本后提交生成时，大模型对**最终脚本**再做一次内容安全审核（色情/违法/仇恨/诈骗等），拒绝则返回 422 + 原因且**不扣生成配额**——防止编辑注入违规内容后直接合成音频（内容安全兜底）
- **一键润色**：SSE 流式返回脚本（逐段出现）；**润色方向指示**：重新润色时可附带一句方向（如"更简短""更口语化""换个开场"），拼入 LLM prompt
- **对话级润色上限（防滥用）**：每个对话最多 **5 个脚本版本**（=5 次润色调用，首个版本计第 1 次；"抽卡"机制，最多抽 5 张）；pro 用户不限额；仅统计 LLM 润色（手动保存不计）；超限返回 429 + 提示；上限配置化
- **重新润色覆盖确认**：已有未保存的手动改动时，重新润色前先确认（防误覆盖）
- **润色语言跟随对话内容自动检测，与界面语言无关**；界面中英双语可切换
- 编辑能力：改文本、增删段落、改说话人、折叠对照原始对话
- **字数统计与上限**：编辑器实时显示总字数（≈ 时长估算）；**全站脚本硬上限 5000 字**——润色输出超限自动截断兜底，生成前校验超限则提示回编辑精简（上限配置化）
- 片头片尾：固定预制音频，按对话语言自动选择（中文/英文），无需配置
- 脚本保存为版本（`scripts.version`），生成用最新版本

### 4.5 生成与发布

- 生成：提交后先过内容安全审核（不通过不扣配额）→ 进度条展示 TTS → 合并 → 上传；完成后可试听
- 发布表单：标题、简介、封面（自动生成默认图 / 上传自定义）、公开/私密
- 发布后：播放页 + 频道页 + RSS 立即可用；触发邀请码发放

### 4.6 频道与内容站

- 频道页 `dailog.fm/@username`：主持人资料、节目列表、RSS 订阅按钮
- 节目页：播放器、简介、润色后脚本全文、下载 MP3、分享、**查看原文（未来）**——对话全文（数据源 `imports.parsed_dialogue`，导入即全量保存），仅供已发布节目公开访问（`getPublishedDialogue` 预留，草稿不可见）
- 首页：最新节目流 + 热门 + 搜索
- 私密节目：仅自己可见，不进入 RSS 与首页

### 4.7 计费与配额

**计费单位 = 脚本字数**（字符数，即 TTS 输入量）：LLM 与 Fish TTS 均按 token/字符计费，成本随字数线性增长，按字数定价与成本结构对齐（固定每期单价会导致短脚本毛利高、长脚本亏本）。

- **脚本硬上限 5000 字**（配置化）：润色输出截断兜底；生成前校验，超限返回提示引导精简
- **润色阶段免费（获客漏斗）**：单次润色成本约 ¥0.04（DeepSeek），不直接收费；防规模化白嫖依赖三重机制——① 质量门拒绝低质对话（垃圾进不来）；② **对话级润色上限：每对话最多 5 个脚本版本**（=5 次润色调用，pro 不限，配置化；仅计 LLM 润色，手动保存不计）；③ 单次润色输入对话量上限（单请求成本封顶）
- **免费层：首期生成 0 扣费**（不限字数，受 5000 字上限约束；完整体验跑通）
- **按期（按字数）付费（基本盘）**：购买字数额度，生成时按**脚本字数**扣减（generate 时读取已保存脚本统计字数，无需额外 LLM 调用）；单价配置化（按千字计）
- **老用户包月（订阅）**：面向已付费/活跃用户，月付后无限字数
- 配额判定在服务端生成入口，防客户端绕过：免费首期不扣；之后按脚本字数扣减；余额不足拒绝并引导购买；订阅无限
- 内容安全审核（§4.4）不通过不扣配额

## 5. 页面清单

### 工作台 SPA（app.dailog.fm）

| 页面 | 内容 |
|---|---|
| `/auth` | 注册（邮箱 + 密码，开放）/ 登录 |
| `/onboarding/channel` | 开通频道：输入邀请码（未开通不能生成/发布） |
| `/onboarding/voice` | 录音引导：录 30–60 秒、试听、重录 |
| `/dashboard` | 我的节目列表（草稿/生成中/已发布）、邀请码、订阅入口 |
| `/episodes/new` | 四步向导：①导入（解析预览/说话人修正）→ ②润色编辑 → ③生成（进度/试听）→ ④发布 |
| `/settings` | 重录声音、改用户名、邀请码管理、订阅状态 |

### 内容站 SSR（dailog.fm）

| 路由 | 内容 |
|---|---|
| `/` | 首页：最新 + 热门 + 搜索 |
| `/@username` | 频道页 |
| `/episode/:id` | 单集页（id = 节目短 ID；episode=单集，语义精确） |
| `/@username/feed.xml` | 播客 RSS |

## 6. 内容与语言规则

- 对话语言检测：LLM 润色前自动检测（zh/en 为主），脚本语言 = 对话语言
- 界面语言：中英切换，与内容无关
- 片头片尾：按对话语言选 zh/en 固定资产
- **单期时长：5–10 分钟**。润色以该时长为目标压缩（对话过长时自动选取最有价值的内容）；仍超上限提示用户拆分或截取；脚本 ≤ 80 段

## 7. 非目标（v2+）

- 文本粘贴 / 导出文件 / ~~分享链接导入~~（分享链接现为唯一导入通道，见 §4.3）
- 背景音乐 / 音效 / 片头片尾自定义生成
- 点赞、评论、关注等社交互动
- 自动内容审核（v1 仅"举报 + 下架"接口）
- 多频道 / 多人节目 / 嘉宾语音个性化
- 播客统计（收听量、订阅数）
- 移动端原生 App

## 8. MVP 成功指标（建议）

- 邀请注册转化：注册页 → 完成录音样本比例
- 漏斗：导入 → 完成生成 → 发布 各步转化率
- 免费首期 → 按字数付费转化率；按期用户 → 包月转化率
- 邀请码发放 → 被使用率（验证增长引擎）
- 单集生成成功率与平均耗时（稳定性）

## 9. 已知边界与限制

- 对话过长需拆分或截取（单期目标 5–10 分钟，约 1200–3000 字）
- 导入依赖各平台分享页公开数据通道（平台接口/页面结构变化时更新采集服务，属持续维护项）
- 克隆音色质量依赖用户录音环境（引导页校验）
- 免费首期 → 多账号刷量风险在邀请制下可接受
- 发布内容为用户生成内容（UGC），需举报下架机制
