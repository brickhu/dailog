# PRD — 产品需求

> 状态：MVP 设计稿（2026-08-02）
> 配套文档：[ARC.md](./ARC.md)（技术架构）· [MRD.md](./MRD.md)（市场与商业）· [AGENT.md](./AGENT.md)（项目总览）

## 1. 产品概述

**把用户与 AI 的对话发布为二人对谈播客**：用户是主持人（用自己的克隆音色），AI 是嘉宾（固定音色）。

- 输入：一段用户与 AI 的对话（分享链接导入，平台清单见 §4.3）
- 加工：LLM 润色成播客脚本（开场白、串联词、理顺口语、压缩长段），用户可编辑
- 输出：一档托管播客（播放页 + RSS 订阅 + MP3 下载）
- 每个用户 = 一个播客频道：`dailogues.com/@username`

## 2. 目标用户与场景

- AI 重度用户：与 AI 的深度对话值得保存、分享、重听
- 内容创作者：把 AI 对话素材变成可发布的音频内容
- 想"被听见"的人：零门槛拥有自己的播客频道（不需录音棚、不需剪辑）

## 3. 核心流程

```
注册（邀请码）→ 录声音样本 → 导入对话 → 润色脚本（可编辑）→ 生成（TTS+拼接）
→ 试听 → 发布（标题/简介/封面）→ 播放页 + RSS 分发 → 社区站可被发现
```

## 4. MVP 功能清单

### 4.1 账户与邀请制

- Supabase Auth：邮箱 + 密码
- 注册必须输入邀请码（未使用且未过期）
- 邀请码来源：
  - **管理员**：CLI 生成（可设过期时间）
  - **用户奖励**：已发布期数 > 3 起，每发布一期节目 +1 邀请码，在设置页可查看/分享
- 边界：已发放邀请码不回收（取消发布不收回）；**前 3 期不补发**；一个码只能用一次

### 4.2 录音与声音

- 注册引导页录音 30–60 秒（波形可视化、试听、重录）
- 质量校验：时长、响度、语音检测（过差提示重录）
- 主持人声音 = 克隆音色（Fish Audio 实时克隆，参考录音样本）；嘉宾 = 平台固定音色
- 可随时重录覆盖

### 4.3 导入（仅 URL 分享链接）

- **唯一导入方式：分享链接粘贴**（不做文本粘贴、不做导出文件——保证所有内容来源可验证）
- **真实性验证（验证码机制）**：平台生成一次性验证码 → 用户将验证码发送到 AI 聊天窗口 → 用户再生成分享链接（**先发码、再分享**）→ 平台抓取并校验验证码存在 → 标记"已验证"
- **平台由抓取矩阵实测决定**（可插拔解析器，按可用性逐个交付）：DeepSeek ✅（已实测）；ChatGPT、Kimi、豆包、通义、Gemini 待 spike 实测；**Claude 不支持**（Cloudflare 质询，MVP 不做无头浏览器方案）
- 解析预览：确认对话结构、修正说话人映射（AI 说话人识别不准确时可手动改）
- 抓取策略：单次抓取 + 缓存 + 限速；解析失败给出明确错误提示

### 4.4 润色编辑器

- **质量审核前置**：打磨前先按审核标准自动预检（轻量 LLM），以下情况直接拒绝并返回原因：对话过短（<3 轮）、纯寒暄无主题、信息量不足、涉及违规内容——不进入打磨与生成（省成本、控质量）
- 一键润色：SSE 流式返回脚本（逐段出现）
- **润色语言跟随对话内容自动检测，与界面语言无关**；界面中英双语可切换
- 编辑能力：改文本、增删段落、改说话人、折叠对照原始对话
- 片头片尾：固定预制音频，按对话语言自动选择（中文/英文），无需配置
- 脚本保存为版本（`scripts.version`），生成用最新版本

### 4.5 生成与发布

- 生成：进度条展示 TTS → 合并 → 上传；完成后可试听
- 发布表单：标题、简介、封面（自动生成默认图 / 上传自定义）、公开/私密
- 发布后：播放页 + 频道页 + RSS 立即可用；触发邀请码发放

### 4.6 频道与内容站

- 频道页 `dailogues.com/@username`：主持人资料、节目列表、RSS 订阅按钮
- 节目页：播放器、简介、润色后脚本全文、下载 MP3、分享
- 首页：最新节目流 + 热门 + 搜索
- 私密节目：仅自己可见，不进入 RSS 与首页

### 4.7 计费与配额

- **免费用户：只能生成 1 期**（完整体验跑通，验证价值）
- **按期付费（基本盘）**：每生成一期 X 元（单价配置化），购买即获得生成额度，随时可用
- **老用户包月（订阅）**：面向已付费/活跃用户，月付后无限期数
- 配额判定在服务端生成入口，防客户端绕过：免费累计 ≤1 期；按期按已购额度扣减；订阅无限

## 5. 页面清单

### 工作台 SPA（app.dailogues.com）

| 页面 | 内容 |
|---|---|
| `/auth` | 注册（邀请码 + 邮箱 + 密码）/ 登录 |
| `/onboarding/voice` | 录音引导：录 30–60 秒、试听、重录 |
| `/dashboard` | 我的节目列表（草稿/生成中/已发布）、邀请码、订阅入口 |
| `/episodes/new` | 四步向导：①导入（解析预览/说话人修正）→ ②润色编辑 → ③生成（进度/试听）→ ④发布 |
| `/settings` | 重录声音、改用户名、邀请码管理、订阅状态 |

### 内容站 SSR（dailogues.com）

| 路由 | 内容 |
|---|---|
| `/` | 首页：最新 + 热门 + 搜索 |
| `/@username` | 频道页 |
| `/@username/:slug` | 节目页 |
| `/@username/feed.xml` | 播客 RSS |

## 6. 内容与语言规则

- 对话语言检测：LLM 润色前自动检测（zh/en 为主），脚本语言 = 对话语言
- 界面语言：中英切换，与内容无关
- 片头片尾：按对话语言选 zh/en 固定资产
- **单期时长：5–10 分钟**。润色以该时长为目标压缩（对话过长时自动选取最有价值的内容）；仍超上限提示用户拆分或截取；脚本 ≤ 80 段

## 7. 非目标（v2+）

- 文本粘贴 / 导出文件导入通道（导入仅支持分享链接）
- 背景音乐 / 音效 / 片头片尾自定义生成
- 点赞、评论、关注等社交互动
- 自动内容审核（v1 仅"举报 + 下架"接口）
- 多频道 / 多人节目 / 嘉宾语音个性化
- 播客统计（收听量、订阅数）
- 移动端原生 App

## 8. MVP 成功指标（建议）

- 邀请注册转化：注册页 → 完成录音样本比例
- 漏斗：导入 → 完成生成 → 发布 各步转化率
- 免费 1 期 → 按期付费转化率；按期用户 → 包月转化率
- 邀请码发放 → 被使用率（验证增长引擎）
- 单集生成成功率与平均耗时（稳定性）

## 9. 已知边界与限制

- 对话过长需拆分或截取（单期目标 5–10 分钟，约 1200–3000 字）
- 导入仅限抓取可达的平台（Claude 因 Cloudflare 质询不支持）；平台反爬规则变更可能临时影响通道，需监控与降级提示
- 克隆音色质量依赖用户录音环境（引导页校验）
- 免费 1 期 → 多账号刷量风险在邀请制下可接受
- 发布内容为用户生成内容（UGC），需举报下架机制
