# PRD — 产品需求

> 状态：MVP 设计稿（2026-08-02）；**本质版（2026-08-13）**：投稿 = URL + 声音采样，制作 = 编辑本地 Agent；**选题标准更新（2026-08-15）**：内容价值四维 → 选题打分器（原创×5/普适×3/冲突×2，满分 100、<60 拒稿）+ 提问保真（原生提问 ≥50%），投稿新增**节目建议**（可选）
> 配套文档：[ARC.md](./ARC.md)（技术架构）· [MRD.md](./MRD.md)（市场与商业）· [AGENT.md](./AGENT.md)（项目总览）

## 1. 产品概述

**dailog——一档将「AI 聊天记录」模拟为真人采访 AI 的播客**（定位与商业逻辑见 MRD §1/§5）。

- **核心差异**：基于 AI 对话生成——原料是**真实发生的人机对话**（不是剧本、不是 AI 编造的对谈），且这份原料人人都已拥有（对话历史）——投稿零门槛的根源
- **形态**：访谈式，每期 5–10 分钟，呈现人与 AI 对话中的精华——按**选题打分器**（原创独特×5 / 普适性×3 / 冲突反转×2，定义见 MRD §1「内容承诺」）
- **来源**：投稿人分享的**真实 AI 对话**（分享链接 + 声音采样）——编辑本地把原始对话加工成访谈结构（开场白、串联词、去芜存菁）
- **制作**：投稿人 = 采访者（自己的克隆音色），AI = 受访嘉宾（dailog 固定品牌声线）→ 发布到 dailog 单 feed，全平台同步更新（v1 RSS 系：Apple/Spotify/小宇宙）

## 2. 目标用户与场景

- AI 重度用户：与 AI 的深度对话值得被听见、被收录（投稿人）
- 想"被听见"的人：零门槛拥有"上过 dailog"的公开作品（投稿人）
- 听众：对真实 AI 对话好奇、想听"人和 AI 聊天精华"的人
- 内容创作者：把 AI 对话素材变成被品牌背书的公开作品（投稿人）

（投稿人画像详见 MRD §2.1）

## 3. 核心流程

```
【投稿人】（dailog.fm）注册（开放，邮箱验证即获投稿资格）→ 投稿：粘贴分享链接（合法性+触达性检查）
+ 声音采样（已有自动填充）+ 节目建议（可选）→ 提交投稿 → /me/submits 查看状态（submitted / rejected / published）
【编辑】（本地 Agent：dailog-editor skill + pnpm editor）查看投稿队列 → 本地拉取网页 → 生成脚本
（dailog 编辑标准）→ 合成语音 → 制作封面 → ✗ 不通过：拒审（附原因，通知投稿人）
→ ✓ 通过：一次性上传发布（published + 期号 + 通知投稿人「dailog 第 N 期」）
【听众】订阅 dailog 单 feed → 收听 / 分享
```

## 4. MVP 功能清单

### 4.1 账户与角色

- better-auth（自托管）：邮箱 + 密码 + 会话；**注册开放 + 邮箱验证**，验证通过即获得投稿资格（**无邀请码**）
- **角色**：`user.role` = user（投稿人）/ editor（编辑）/ admin（管理员）（账号级字段在 user 表）；**投稿人端在 dailog.fm（site）**，注册即可投稿；**编辑在本地 Agent 工作**（dailog-editor skill + `pnpm editor`），不部署任何编辑前端
- 边界：垃圾投稿的兜底 = 邮箱验证 + 人工审核队列；v1 不做投稿频率限制，观察队列垃圾率后再加（配置化）

### 4.2 人设（主持人信息 + 声音采样）

- **人设 = 投稿人的主持人配置**：称呼 + 风格描述（可选）+ 声音采样（30 秒内，波形可视化、试听、重录）
- **配置时机：投稿确认环节**；已配置过则自动填充、可修改
- 声音 = 投稿人克隆音色（编辑本地 TTS 零样本克隆——录音样本 + 朗读文案随 TTS 请求携带，**重录即时生效**）；嘉宾 = dailog 固定品牌声线（guests 采样 / 音色模型 id）
- 人设可随时在设置页重录/修改

### 4.3 投稿（URL + 采样，本质版）

- **投稿 = 分享链接 + 声音采样 + 节目建议（可选）**：服务端只做**基本合法性（http/https 格式）与触达性（网络可达）检查**，不做内容采集——内容拉取由编辑本地 Agent 完成（浏览器可交互处理反爬，正是去掉服务端采集的原因）
- **节目建议（可选，≤500 字）**：投稿人填写的选题参考（主题/角度/风格倾向），编辑生成脚本时按三层处理——明确边界（"不要/别/避免"等否定指令）必须遵守、冲突宁可拒稿；明确风格倾向 → 脚本生成后比对贴近；选题视角仅作切题参考，无参考价值可忽略
- 分享链接覆盖所有可访问的 AI 对话分享页（不绑定平台清单——编辑本地可处理任何来源）
- 投稿状态机（submissions）：`submitted`（待审核）→ `rejected`（拒审，附原因）/ `published`（编辑上传成品后）
- 并发上限：待审 ≥5 条 → 429 引导等待；同 user+url 重复提交 → 返回已有投稿
- ~~分享链接采集服务（importer）~~、~~浏览器扩展采集~~：**已删除**（2026-08-13 架构极简改造）

### 4.4 审核与制作（编辑本地 Agent）

- **编辑本地完成全部制作**：拉取网页内容 → 按 dailog 编辑标准生成脚本（**选题打分器**先于切题——
  原创独特×5 / 普适性×3 / 冲突反转×2（各 1-10 分），满分 100，**总分 <60 拒稿**；
  通过后切题润色，**提问保真**：用户原生提问含义不得歪曲、用户台词原生提问占比 ≥50%；
  拒绝条件：打分 <60、对话过短、纯寒暄无主题、与投稿人明确边界冲突、涉及违规内容）
- **脚本生成标准**（完整规范见 `.agents/skills/dailog-editor/SKILL.md`）：5–10 分钟（约 1200–3000 字）、朗读向短句、
  真人对话感（[break] 停顿 / 反馈接话 / 比喻 / 打断 / 调侃 / [laughing]）、**固定开场白结构**（问候 → 自我介绍 →
  欢迎来到 Dailog 并说明形态 → 引出 AI 嘉宾；信息点不可变）、**Fish Audio S2 情绪标签**（[happy]/[sad]/[excited]/
  [whispering]/[sighing] 等，随文本直达 TTS）、隐私模糊化
- **批量流程**：`batch` 批量提取分组 → 处置 → 自动质量检查/脚本生成（quality_failed 落盘）→ `batch-scripts` 分组 → 处置 → 选号 `produce` 制作流水线（语音试听、发布确认两个确认点）→ `publish`（状态 published + 通知 + 邮件 + 草稿清理）；`overview` 一键工作台概要
- **脚本可多次迭代**（编辑与 Agent 对话调整：更简短 / 换个开场 / 换情绪风格），草稿存本地（`.dailog-editor/drafts/`）
- 语言跟随对话内容自动检测，与界面语言无关

### 4.5 发布与拒审（编辑本地 → 服务端）

- **制作**：Fish TTS（host = 投稿人采样克隆 / guest = 品牌声线）→ ffmpeg 合成（intro + 对话 + outro，段间自然停顿）
  → 节目信息（标题/简介/标签/封面关键词）→ 封面（Pexels 图库搜索，可多组关键词选最优）
- **发布**：编辑确认后**一次性上传**（音频 → R2，元数据 → episodes 表）→ 直接 published（期号 max+1）→
  播放页 + RSS + 投稿人通知（站内 + 邮件「dailog 第 N 期」）即时生效
- **试听前置**：发布前本地试听成品（音色/断句/情绪标签验证）——发布是外发动作，编辑把关
- **拒绝**：编辑拒审（原因必填，投稿人 /me/submits 可见 + 邮件）
- **精选标记**：编辑可标记精选节目（`episodes.is_picked`）——首页首屏播放器与 `/discover/picked` 共用该池

### 4.6 频道与内容站

- **dailog 单一品牌频道**（首页 = 频道本体）：最新节目流 + 精选 + 搜索
- 节目页：播放器、简介、下载 MP3、分享
- **投稿人主页 `dailog.fm/@username`**：投稿人资料 + 被收录作品集（"上过 dailog"）
- **单 feed RSS**（`dailog.fm/feed.xml`）：进 Apple/Spotify/小宇宙；分语言 feed（zh/en）为 v2
- 未收录 = 无公开节目（投稿仅自己可见状态），无私密节目概念

### 4.7 成本预算（v1 无创作者收费，编辑驱动）

**v1 无任何收费点**——投稿免费、收录免费；成本控制 = **编辑决策（用稿率）**（商业逻辑见 MRD §5）。

- **编辑本地按量成本**：脚本生成（LLM 几美分/期）+ Fish TTS（~¥0.6/期，免费模型 `s2.1-pro-free` $0）+ Pexels 免费额度
  ——**编辑的用稿率 = 成本总开关**（例：100 投稿/天，收录 20 期 ≈ ¥15–30/天）
- **服务端成本极小**：投稿与发布都是轻量操作（无 LLM；TTS 收敛为服务端统一端点 `/v1/editor/tts`，Fish TTS 按量），Railway ~$5–15/月
- **内容安全**：编辑本地制作时把关（脚本生成标准含违规内容拒绝；发布前人工确认）——品牌频道命门（MRD §5）
- **v2 听众侧变现**（频道资产验证后，MRD §5）：Apple/小宇宙会员 + 打赏与入选投稿人分成——不在 MVP 范围

## 5. 页面清单

### 内容站 SSR + 投稿人端（dailog.fm，无编辑路由——编辑在本地 Agent）

| 路由 | 内容 |
|---|---|
| `/` | 首页（landing）：hero（tagline + 一句话 what it is + 「立即投稿」CTA）+ **推荐节目滚屏**（每屏 4 条、最多 5 屏；末屏不足 4 条灰块补齐；异步加载骨架屏占位）+ 统计卡片 + FAQ |
| `/discover` | 探索页：新 / 热 / 精 / 荐 |
| `/submit` | **投稿流程**：①粘贴分享链接（前端基本校验）→ ②配置人设（称呼/风格可选 + 声音采样必填，已有自动填充）+ 节目建议（可选）→ ③提交投稿 |
| `/me` | 个人中心入口 |
| `/me/episodes` | **我的节目**：已发布节目列表 + 下架/重新上架（切换公开状态，仅自己可见） |
| `/me/submits` | 我的投稿及状态（审核中 / 投稿失败 / 已发布，附拒审原因） |
| `/me/notifications` | 站内通知（拒审 / 节目上线） |
| `/me/favorites` | 我的收藏 |
| `/hosts` / `/guests` / `/guest/<id>` | 热门主播 / 常驻 AI 嘉宾列表 / 嘉宾详情（含参与节目） |
| `/settings` | 账户与设置（邮箱/昵称/档案/声音采样/密码）（`/account` 为兼容别名） |
| `/login` | 登录/注册（邮箱验证） |
| `/@<name>` | 投稿人主页（@slug = 账号昵称 user.name，注册时应用层强制唯一，冲突加随机后缀） |
| `/episode/<slug\|id>` | 播放页（单集；slug 与旧 id 均可解析，旧链接 301） |
| `/feed.xml` | 播客 RSS（单 feed，进 Apple/Spotify/小宇宙） |

## 6. 内容与语言规则

- 对话语言检测：编辑本地生成脚本时自动检测（zh/en 为主），脚本语言 = 对话语言
- 界面语言：中英切换，与内容无关
- 片头片尾：本地资产（intro/outro，编辑按语言选用；缺失降级只拼主对话）
- **v1 单 feed 混排多语言**（语言跟随对话内容）；分语言 feed（zh/en）为 v2
- **单期时长：5–10 分钟**（约 1200–3000 字）；脚本 ≤ 80 段

## 7. 非目标（v2+）

- 文本粘贴 / 导出文件 / ~~服务端采集~~（采集在编辑本地，见 §4.3/§4.4）
- 背景音乐 / 音效 / 片头片尾自定义生成
- 评论、关注等社交互动（点赞/收藏已实现：详情页图标 + 计数；未登录点击跳登录——`POST/DELETE /v1/episodes/:id/like|favorite`）
- 自动内容审核（v1 编辑人工把关 + "举报 + 下架"接口）
- 投稿人子频道 / 多语言分 feed（v2，矩阵式扩张）
- 播客统计（收听量、订阅数）
- 移动端原生 App

## 8. MVP 成功指标（建议，投稿制）

- 投稿漏斗：注册 → 邮箱验证 → 提交投稿 各步转化率
- 投稿-收录漏斗：投稿量 → 编辑处理量 → 收录量（编辑用稿率，验证收录标准松紧与成本预算）
- 编辑吞吐：投稿到审核的平均时效（人工驱动的瓶颈指标）
- 入选者行为：入选后自发分享率（荣誉驱动假设，MRD §9-3）
- 听众指标：频道订阅数、单集收听/完播、回访（频道资产验证，MRD §9-4）
- 供给可持续：每周投稿量 vs 每周收录期数（维持 1–2 期/周，MRD §9-5）

## 9. 已知边界与限制

- 对话过长需拆分或截取（单期目标 5–10 分钟，约 1200–3000 字）
- 分享页反爬由编辑本地浏览器处理（交互式，天然绕过数据中心 IP 拦截）；拉取失败如实汇报
- 克隆音色质量依赖用户录音环境（投稿确认环节校验；发布前本地试听）
- 成本由编辑决策 + 本地按量控制（MRD §5）；多账号刷投稿由邮箱验证 + 人工审核兜底（垃圾只占队列，不产生制作成本）
- 收录内容为用户生成内容（UGC），需举报下架机制；**品牌频道 = 品牌风险面，编辑人工把关为命门**（MRD §5）
