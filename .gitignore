node_modules/
dist/
.env
.env.*
!.env.example
scripts/spikes/out-limits/
sample-voice.*
apps/extension/dist/
data-test/

# zcode 会话内部文件
.zcode/

# db-ops 操作日志（审计留痕）
.dbops/logs/

# 运行时数据（用户录音样本/生成音频）——隐私，绝不入库
services/api/data/

# db-ops 本地配置（连接串/快捷指令/日志）——不上传
.dbops/

# spike 生成产物
scripts/spikes/out-*.mp3

# 误放的项目内 Chrome 用户数据目录（本地浏览器配置，非项目产物）
poc-profile/

# 容器内 pnpm install 生成的 store（误入挂载工作区）
.pnpm-store/

# 编辑本地 Agent 配置（密钥）与草稿（中间产物/音频）——不上传
.dailog-editor/

# dailog-editor 子工程打包产物（构建生成；构建命令见 tools/dailog-editor）
.agents/skills/dailog-editor/

# vinxi 运行时生成的配置临时文件
apps/site/app.config.timestamp_*.js

# wrangler 本地运行缓存（miniflare state，运行生成）
.wrangler/
