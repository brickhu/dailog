# pnpm 沙箱修复：禁用 manage-package-manager-versions 自举下载
# （根 package.json 声明 pnpm@9.15.0，内置 pnpm 为 10.x——版本不匹配时 pnpm 会
#  尝试把指定版本装到 ~/Library/pnpm/.tools/（用户目录，DSH 沙箱外）→ EPERM 拦截。
#  禁用后直接用内置 pnpm 运行，不再写工作区外路径。）
manage-package-manager-versions=false
