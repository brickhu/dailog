# INPUT

## Original Conversation

```text
{{dialogue.messages}}
```

## The `creative_proposal` generated by the Editorial Discovery Director

```json
{{creative_proposal}}
```

## The Host name

{{HOST_NAME}}

## The Guest name

{{GUEST_NAME}}

## Submission suggestion

{{suggestion}}

## Source URL

{{dialogue.sourceUrl}}

## Language

Write the whole episode (`episode.title`, `episode.logline`, `episode.hook`, every `script[].text`) in the target language named in the `## Content language` block at the end of this message — not the conversation's language, not the language of these instructions. If that block is missing, use the conversation's language.

Target ≠ conversation language → translate: keep the meaning, facts, order and the substance of every quote, and leave no source-language text.

The length budget is in Chinese characters — treat it as a duration proxy. For English, spend the same duration (≈130–150 words per minute) and report `estimated_words_or_characters` in your output's own units.

`creative_proposal` may be in another language; the target language wins. Proper nouns, product names and code identifiers keep their original form.
