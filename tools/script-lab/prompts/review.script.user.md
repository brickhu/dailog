# 创作任务（审题第2轮——本轮只做脚本创作，不再执行第1轮的判断/打分/拒绝流程）
评分={{score}}，≥6.5（已通过审核），审题流程已结束。这篇人机对话适合包装为真人采访 AI 的二人对谈脚本（约 5-10 分钟）：
- 主线话题：{{selection.main_topic}}
- 听众价值锚点锁定到「{{selection.category}}」

创作建议（round1 审题结论，创作时直接遵循）：{{selection.advice}}

**写作顺序：先做第 1 步提取创作锚，再按第 2 步写脚本。**

**第 1 步 · 提取创作锚（内部进行，不要输出到最终 JSON）——只从对话原文提取，禁止编造：**
1. **本期话题句**：一句话说清这期聊的具体内容（具体到能填空："这期聊的是___"），必须来自原文真实话题；禁止"一个想法/某个问题/那件事"式无所指的概括；
2. **承重墙原话**：逐字挑出 2-4 条用户原话/关键时刻（真实提问、顿悟、戳心句），写作时逐字保留或原词引用；
3. **guest 真实立场**：AI 在原文中实际说过的关键观点/行为（拒绝、反问、澄清、给结论等）。

**第 2 步 · 创作脚本：**
- 点题第 2 句必须用大白话把第 1 步的「本期话题句」说出来——**话题不落地不开场**；
- host 每一句发问都要锚定第 1 步的承重墙原话；guest 回应锚定其真实立场；
- 脚本里任何"这个想法/你说的X"都必须能指回第 1 步已提取的具体内容，禁止凭空悬空指代；
- 按前一条 assistant 消息的「脚本输出规范」，直接输出脚本对象（segments 完整非空，不要包 scripts 数组）。

# 数据
对话全文已在前序消息完整提供（唯一事实来源）。
主持人：{{host}}
嘉宾：{{guests}}
投稿建议：{{suggestion}}

# 输出形状（最后指令）
输出 JSON 的最外层就是这个对象：
{"segments": [{"speaker": "host", "text": "开场白台词"}, {"speaker": "guest", "text": "嘉宾回应台词"}], "host": "主持人称呼", "guest": "嘉宾名", "lang": "zh"}
**最外层不要出现 "scripts" 键，不要空 segments。**
