# script-lab LLM 配置（复制为 tools/script-lab/.env 后填写；.env 已被 gitignore）
# 优先级：命令行 flag > 环境变量 > 本文件 > 仓库根 .env
#
# 必填（二选一）：
# DEEPSEEK_API_KEY=sk-xxx
# LLM_API_KEY=sk-xxx
#
# 采集兜底（可选，默认关）：
# 直连/代理都拿不到，或拿到壳页提取不到消息时，交给 microlink 渲染一次再解码。
# 只在需要时开（编辑救急 / 本机没有 Chromium）；开了会把投稿链接交给第三方渲染。
# DAILOG_MICROLINK=1            # 1/on 开；0/off 关；不设时——填了 MICROLINK_API_KEY 即视为开
# MICROLINK_API_KEY=xxx         # 可选：付费层 key（免费层 25 次/天，无 key 也能跑）

# 可选：
# LLM_BASE_URL=https://api.deepseek.com
# LLM_MODEL=deepseek-chat
# LLM_TEMPERATURE=0.7
# LLM_MAX_TOKENS=
